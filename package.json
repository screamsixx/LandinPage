{
  "name": "pascal-landing",
  "version": "1.0.0",
  "author": "Pascal",
  "license": "MIT",
  "scripts": {
    "prebuild": "rimraf dist",
    "build": "eleventy && npx cpx \"php/**/*\" dist/php",
    "start": "eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^0.11.0",
    "rimraf": "^3.0.2"
  }
}